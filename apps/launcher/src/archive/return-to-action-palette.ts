// /**
//  *
//  */

// import { Action, Icon } from "@raycast/api"

// export function ReturnToActionPaletteAction({ resetState }: { resetState: () => void }) {
//     return <Action title="Return to Action Palette" icon={Icon.ArrowLeftCircle} onAction={resetState} shortcut={{ modifiers: ["cmd"], key: "arrowLeft" }} />
// }
