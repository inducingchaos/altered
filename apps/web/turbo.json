{
    "$schema": "https://turborepo.com/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "env": [
                "DATABASE_URL",
                "AUTH_SECRET",
                "AUTH_GOOGLE_ID",
                "AUTH_GOOGLE_SECRET",
                "CONFIG_ENV",
                "NODE_ENV",
                "PORT",
                "PROVIDER_OPENROUTER_SECRET"
            ]
        }
    }
}
