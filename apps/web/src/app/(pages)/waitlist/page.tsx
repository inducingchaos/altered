/**
 * @todo [P2] Build out page if needed, remove when MVP is complete.
 */

import { Button, Container, P, Section } from "~/components/ui/_legacy"

export default function WaitlistPage() {
    return (
        <Container>
            <Section>
                <P>Nothing here yet.</P>
                <Button>go back</Button>
            </Section>
        </Container>
    )
}
