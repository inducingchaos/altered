{
    "$schema": "https://turborepo.com/schema.json",
    "extends": ["//"],
    "tasks": {
        "build": {
            "env": [
                "AI_GATEWAY_API_KEY",
                "BLOB_READ_WRITE_TOKEN",
                "REDIS_URL",

                //  TEMPORARY: COPIED FROM altered-web

                "DATABASE_URL",
                "AUTH_SECRET",
                "AUTH_GOOGLE_ID",
                "AUTH_GOOGLE_SECRET",
                "CONFIG_ENV",
                "NODE_ENV",
                "PORT",
                "PROVIDER_OPENROUTER_SECRET"
            ]
        }
    }
}
