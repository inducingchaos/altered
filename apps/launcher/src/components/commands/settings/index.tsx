/**
 *
 */

import { Form } from "@raycast/api"

export default function Settings() {
    return (
        <Form>
            <Form.Description title="Placeholder" text="Nothing here yet." />
        </Form>
    )
}
